controllers/*Controller.php: lấy model (hoặc không) rồi truyền giá trị tới view

models/*Model.php: query từ database để dùng cho controller

views/*.php: hiển thị thông tin cho từng trang giao diện (làm như html)

index.php: đóng vai trò như router, thêm giao diện thì cứ thêm vào